# G6 — negative-result confirms (wavefront, adaptive), 2026-06-13

Two abandoned optimisations re-checked at the calibrated operating point. Both are huge-margin
negatives already in the dev ledger; G6 confirms they survive the campaign's correct caps. 150 W (both
arms at the same power → ratio robust; the margins are the point).

## Adaptive sampling — CONFIRMED net loss (fresh data)

Driver `scripts/campaign/run_g6_adaptive.sh`; build `~/winbins/exe_adapt`
(`ENABLE_ADAPTIVE_SAMPLING=true`, `ADAPTIVE_THRESHOLD=0.01`). cloud-meadow, 64 spp, 3 seeds, vs the
banked 1024-spp ours-MIS GT (`g1_seeds/cuda_seed*`). `g6_adaptive/times.csv`.

| arm | median time | mean RMSE vs GT | 0 overflow |
|---|---|---|---|
| adaptive (ON, 1% thr) | 23.94 s | **0.1706** | yes |
| uniform (stock) | 23.77 s | **0.1706** | yes |

**Identical quality, adaptive 0.9 % slower** — and it allocates an extra `W·H·16 B` variance buffer
(measured separately: not free, see `vram.md` mechanism). Mechanism: at a 1 % convergence threshold,
*no* pixel on this high-variance cloud meets the early-stop test, so adaptive degenerates to uniform
sampling at full sample count plus the per-sample Welford overhead. It cannot win on the high-variance
volumetric workload it was meant to help. Confirms the §8.30 verdict (net loss, default-off).

## Wavefront path tracer — re-run BLOCKED; cite dev measurement

The wavefront toggle lives only on `deprecated-wavefront-phase1` (tip `593d927`, "implemented +
measured DEAD END"). Built both arms (`THESIS_WAVEFRONT` OFF/ON) in a worktree, but **the branch's
binaries fault on the current toolchain**: an illegal memory access at `cuda/stream.h:40` on *every*
launch, including the asset-free `single_gaussian_validation` and with `white_constant` (so it is not
the env-IS path or the post-reorg asset paths — those were symlinked in and the env loaded). The branch
predates the asset/env reorganisation and the current OptiX/driver, and was abandoned; resurrecting it
is not worth it for a confirm.

The dead-end is documented at **FINDINGS §8.34**: the host-driven per-bounce wavefront is **100–1400×
slower** than the megakernel (the global per-bounce RayState sync dwarfs any divergence saving). That
margin is enormous and power-/cap-independent; the campaign caps would not move it. **The megakernel is
the production architecture** (Ch 4). Worktree removed after the attempt; the branch is preserved.

## Thesis wiring
- Adaptive → `sec:autopsies` / negative-results ledger: "equal quality, ~1 % slower, extra memory; at
  1 % threshold it degenerates to uniform on high-variance media." Confirmed at calibrated caps.
- Wavefront → keep the §8.34 100–1400× figure; note the re-run was blocked by the abandoned branch not
  building/running on the current toolchain (still a documented dead-end).
