# MSc thesis — full examiner review (2026-06-15)

**Session:** `abee57dc-898f-4d57-8c69-c309db33f10d`
**Scope:** abstract + Ch 1–8, all figures, the bibliography they cite, and the supporting
campaign records — cross-checked against the source (`device/`, `src/`, `test/`),
`thesis/FINDINGS.md`, and the reference (`volprim`). **Every load-bearing number was re-derived
from banked seed EXRs** (clock-independent: variance/RMSE/mean/bias/VRAM — see §0). The GPU power
cap (150 W) was respected; no frame times were re-measured.
**Method:** decomposed across seven focused passes (Ch 4 vs code; Ch 5 validation logic; Ch 7/8 vs
records; Ch 2/3/6; whole-thesis honesty; whole-thesis consistency/figures; numerical reproduction).
Per-pass detail is in `thesis/reviews/scratch/*.md`.
**Build:** `latexmk -pdf thesis.tex` clean — 0 errors, 0 undefined refs, 0 multiply-defined labels,
69 pp, 12 cosmetic overfull hboxes (largest 45.8 pt).

---

## Top line

The thesis is in strong shape and largely submission-ready. **No load-bearing number, algorithm
description, or unbiasedness claim is wrong** — the 59× headline, the +156 % NEE bias, the flat-env
deflation, the absorption ladder, the mean-convergence evidence, and the memory win all reproduce
(§0). The two new Ch 4 derivations (`sec:analytic-od`, `sec:adt`) are mathematically correct and the
argmin description matches the code line-for-line. The de-hyping is consistent and, in places
(§7.1, the flat-env rung, the icosphere autopsy), exemplary.

The **Blocking** list is **five localized factual/traceability slips** — not structural or numerical
problems. Two I found directly (a residual "the reference sorts" misattribution the earlier purge
missed; one false methodology sentence the author already queued for removal). **Three more were
surfaced by an independent parallel review of the same draft (session `0a76adc7`) and I confirmed each
against the source** (a mislabelled correctness gate, a wrong table-caption ratio, and an untraceable
performance number). All five are one-line fixes; everything else is Should-fix scoping/precision and
Polish.

One inter-agent disagreement I adjudicated against the code: **RIS is *not* unreachable** — see §6.

> **Cross-validation note.** A second, independently-run review (`thesis/reviews/2026-06-15-0a76adc7.md`,
> a 26-agent adversarial pass) landed in parallel. The two reviews **reproduced every headline number
> identically** (58.6× / +156.1% / +0.4% / 5.0× / 68.3× / N^0.40) — strong mutual confirmation that the
> core claims hold. The parallel pass was more thorough on traceability and caught three Blocking items
> mine missed (now folded in as B-3/B-4/B-5 below, each re-verified by me). Its full Should-fix list
> (24 items, heavily overlapping mine) is worth reading alongside this doc; I fold in only the
> high-value items mine lacked (SF-14…SF-18) to avoid duplication.

---

## 0. Reproduction — load-bearing numbers re-derived from banked EXRs

All recomputed on-machine via `tools/refs/.venv` (numpy + OpenEXR) from the banked seed renders;
no GPU used. Full method/numbers in `scratch/reproduction.md`.

| Measurement | Thesis / record | Recomputed | Verdict |
|---|---|---|---|
| ours-MIS k_raw / k_clip999 | 1.99 / 1.887 | **1.985 / 1.8872** | PASS |
| Mitsuba-analog k_raw / k_clip999 | 3899 / 110.6 | **3898.6 / 110.594** | PASS |
| **59× clipped equal-quality (k-ratio, equal time)** | ~59× | **58.6×** (110.594/1.8872) | PASS |
| Mitsuba-NEE energy bias vs analog GT | +156.1 % | **+156.1 %** (0.8199/0.3201) | PASS |
| ours-MIS bias vs analog GT | +0.4 % | **+0.41 %** | PASS |
| flat-env means agree (correctness) | 0.02 % | **0.002 %** | PASS |
| flat MIS variance ratio (ours/Mits) | "~10×" | **8.2×** (record rounded 0.10/0.01) | PASS* |
| flat analog variance ratio (ours/Mits) | ~5× | **4.98×** | PASS |
| scatter-ladder mean agreement | 0.4 % | **0.41 % (0.33σ), s.e.-ratio 68×** | PASS |
| absorption single/pair/cloud signed-mean bias | ≤~1e-4 | **+6.5e-4 / +1.4e-4 / +1.2e-6** | PASS |
| VRAM 578 / 838 / 1200 MiB; GAS/prim | — | **578 / 838 / 1200; 0.162 KB/prim (0.018 % of peak)** | PASS |

\* The "10×" is the record's rounding of k_clip 0.10/0.01; the exact unrounded ratio is 8.2×. The
qualitative claim holds (see SF-9).

**Two reproduction nuances worth wiring into the prose** (both Should-fix, listed below): the 59×
depends on *which* clipped-variance convention is used (SF-8), and the "~10⁻⁴ unbiased" sentence
telescopes two distinct evidence tiers (SF-7).

---

## 1. Blocking (wrong claims / internal contradictions — all localized, all one-line fixes)

> Calibration: these are factual corrections, not load-bearing-claim failures. The numbers and
> algorithms are verified sound. But each is *literally wrong in the current text*, and an examiner
> who knows `volprim` (B-1) or reads `FINDINGS.md` (B-2) would catch it.

**B-1. The residual "the reference sorts" misattribution — the earlier purge missed Ch 4 and Ch 6.**
Commit `38c3d25` corrected the abstract, Ch 3, and Ch 8, but three sites still attribute a *sort* to
the reference, which marches + running-min-selects + root-finds — it never sorts (verified again
against `volprim/integrators/common.py`):
- `04-architecture.tex:7` — "marches the ray segment by segment through **the sorted sequence of
  primitive boundaries**". This directly contradicts the same chapter's `:10` ("with **no sorting**,
  no marching, and no root-finding") and `:286` ("sort-free spirit"). Internal contradiction.
- `06-optimization.tex:193` — "removes the reference's **per-bounce sort** and root-find outright".
- `06-optimization.tex:202` — `tab:complexity` row "**Boundary sort (scatter)** $O(H^2)$ /
  $O(H\log^2 H)$". The $O(H^2)$ is right (running-min scan over an unsorted list), but the label and
  the $O(H\log^2 H)$ "sorted alternative" impute a sorting algorithm.
**Fix:** mirror the wording already applied in Ch 3 / the `fig:pipeline` caption — e.g. Ch 4:7 "marches
from one boundary to the next, selecting each by a running minimum, and root-finds the scatter
distance"; Ch 6:193 "removes the reference's per-bounce boundary scan and root-find"; rename the
table row "Boundary selection (scatter)" and drop the `$O(H\log^2 H)$` sorted variant.

**B-2. `06-optimization.tex:15` — "Optimisation began with profiling, not guessing" is false.**
`FINDINGS §8.5` (the first optimisation analysis) explicitly labels its levers **"never profiled"**;
the first NSight Compute profile is `§8.28`, much later. The sentence also contradicts this chapter's
own reasoning-first A1 autopsy. *(This is already HANDOFF Ch 6-rework item 3 — "Kacper: false +
pretentious" — just not yet applied.)*
**Fix:** delete the sentence; open `sec:bottleneck` with the profile result itself ("NSight Compute
locates the bottleneck firmly in local-memory traffic…").

> The next three were surfaced by the parallel review (`0a76adc7`) and **independently re-verified by me
> against the source/records** — my own 7-pass run missed them.

**B-3. `07-results.tex:182` + `fig:generalisation` caption (`:197-198`) — the tornado/explosion
correctness gate is mislabelled "absorption"; it was scattering.** The prose says "Each is rendered in
**absorption** under a constant-white environment", but the record is explicit: `asset_parity.md`
states the gate used "**uniform albedo 0.9 scattering**" with a parenthetical "*Scattering, not
absorption … our `asset_validation` cannot force albedo 0 for these scattering-albedo assets … a
matched uniform-0.9 scattering gate is the natural regime.*" It is also an **internal contradiction** —
the *same two assets* are correctly labelled scattering (albedo 0.9, white-constant, σ-scale 10) in
`tab:asset-cost` (`:263-264`). *Fix:* change "absorption" → "uniform-0.9 scattering" in both the prose
(`:182`) and the caption (`:197`); the ratios (0.99911 / 1.00006) are unchanged.

**B-4. `06-optimization.tex:417-418` (`tab:overlap` caption) — the "32×" baseline is wrong (should be
6×).** The caption says "the *fine* fit packs **$32\times$ more primitives than the *dense* fit** yet
overlaps an order of magnitude *less*". From the table's own rows, fine = 24576 and dense = 4096, so
the ratio is **6×** (24576/4096); 32× is fine-vs-*coarse* (24576/768). The "order of magnitude less"
overlap (529 vs 57 = 9.3×) is the right dense-vs-fine comparison, so the sentence mixes two baselines,
and it disagrees with the body (`:393-394`), which correctly uses dense-vs-fine. *Fix:* "**$6\times$
more primitives than the *dense* fit**".

**B-5. `04-architecture.tex:357-360` (`sec:gpu-impl`) — the safe-vs-tuned frame-time penalties trace to
no banked record.** The text states a universal "safe" build (512/512) "measured **$+13\%$ frame time
on the cloud, $+7\%$ on the explosion, and ${\sim}+3\%$ on the tornado and bunny** against the
per-asset-tuned builds." An exhaustive search of `results/campaign/` finds **no** per-asset
safe-vs-tuned *timing* A/B: `vram.{csv,md}` record memory only (flat 1200 MiB, 0 overflows, no times),
`scaling.csv` times only the tuned binaries, and the only banked hit-cap timing A/B is `caps_ab.md`
(active-set 64↔128 null; hit-cap "128→256 ~6× on the cloud"). These four percentages therefore violate
the thesis's own "every number traces to a record" standard, and the precise "+13%" sits in apparent
tension with `caps_ab.md`'s much larger "128→256 ~6×" hit-cap penalty (worth reconciling). *Fix:* bank
the interleaved safe-vs-tuned A/B as a `results/campaign/*.md` record and cite it, or drop the four
percentages and keep the *memory* claim (which is fully banked) plus the traceable `caps_ab.md`
hit-cap number. **(Timing is clock-dependent — this also needs the locked-clock window if re-run.)**

---

## 2. Should-fix (scoping, precision, weak-evidence, figure/caption, structure)

**SF-1. `08-conclusion.tex:11` — "a *novel* rendering architecture": a regression vs the rest of the
thesis.** The abstract ("a direction the reference's authors anticipated but did not implement") and
Ch 4 §4.4 (Condor attribution) both hedge this correctly; the conclusion dropped the hedge.
*Fix:* "a rendering architecture — a direction the reference's authors anticipated but did not
realise — with two complementary halves…".

**SF-2. `06-optimization.tex:339` — adaptive sampling "~2× slower at equal quality" headlines the
worst operating point.** The controlled equal-quality campaign re-run (`g6.md`) measured **0.9 %
slower**; the ~2× only appears when the cap is raised to chase higher quality (`§8.30`).
*Fix:* "roughly break-even at the operating point (0.9 % slower), degrading to ~2× slower when pushed
to higher quality settings — a net loss either way."

**SF-3. `07-results.tex:36` — the "~3× faster per sample (2.85 s vs ~8.5 s)" parenthetical is the
*analog* mode but sits in a clause time-anchored to the cloud MIS arm; a skimming examiner could read
it as the production arm.** *Fix:* add "(pure-analog mode)" to the parenthetical. (The only genuine
ambiguity in Ch 7 — the rest of §7.1 is a model of honest scoping.)

**SF-4. `01-introduction.tex:22` — "to render … correctly and substantially faster than the
reference": the achieved "substantially faster" is environment-IS-specific** (the core sampler is net
~0.6× on flat). This reads as a *goal* statement, so it is defensible as written — but given the
thesis's scoping elsewhere, one clause removes any reader over-extrapolation. *Fix (optional):* "…and
substantially faster at the production, environment-lit operating point" — or leave as a goal and rely
on §7.1's scoping. Lower priority than the honesty pass first implied.

**SF-5. Five figures are never `\Cref`'d in the body**, so as floats they don't formally earn their
place: `fig:mc-integ`, `fig:gmms`, and the three Ch 4 TikZ schematics (`fig:optical-depth`,
`fig:argmin`, `fig:per-ray-state`). The TikZ figures are intended and content-justified by adjacent
prose; they just lack an explicit reference. *Fix:* one `\cref` each at the point the prose discusses
them.

**SF-6. `04-architecture.tex:414` — bunny caps "320 suggested" collides in one sentence with the
`tab:overlap` value it cites (245).** Both are correct (320 = margined cap suggestion; 245 = raw
max-overlap, per `caps_table.csv`) but they read as a contradiction. *Fix:* "…the estimator's raw
overlap of 245 (Ch 6, `tab:overlap`), margined up to a 320 cap".

**SF-7. The "~10⁻⁴ unbiased" sentence telescopes two distinct evidence tiers** (`05-validation.tex`,
`fig:scattering-ladder` + surrounding prose). Reproduction confirmed the *figure* shows whole-image
**mean convergence at ~10⁻³** (0.41 %, 0.33σ, s.e.-ratio 68× — all 64-spp seeds can support), whereas
the **~10⁻⁴ / +2×10⁻⁴ cluster-core / +1×10⁻⁴ cloud** numbers are a *separate higher-budget*
measurement (`FINDINGS §8.3/§8.4`, 16k–64k spp, core binning) that is correctly **not** recomputable
from the 64-spp seeds. The claim is *supported* (global systematic resolved to −1.6e-5 ± 8e-6;
residual ≤0.16 % peak) — but the single sentence reads as if the ladder figure delivers the 10⁻⁴.
*Fix:* separate them — "the ladder figure shows image-mean agreement to ~10⁻³ at 64 spp; the residual
core bias is bounded to ~10⁻⁴ by the dedicated high-sample analysis of §5.x."

**SF-8. Define the equal-quality metric *and its clip convention* explicitly — it swings the headline
~25×.** Reproduction showed the 59× rests on the **raw-radiance 99.9th-percentile clip** documented in
`run_g1_flat.sh:49` (→ k_clip 1.887/110.6 → 58.6×); clipping the *variance map* instead gives ~1520×.
The thesis uses the conservative (smaller-number) convention — good — but neither "k" nor "clipped
variance" is currently defined where the headline lands. *Fix:* one sentence in Ch 7 (or a Ch 5
methods note): "k = mean per-pixel inter-seed variance (ddof = 1) × spp; the clipped variant clips
per-pixel radiance at the 99.9th percentile before computing variance, discounting sparse fireflies."
This pre-empts the obvious "which clip?" defense question and shows the headline uses the conservative
choice.

**SF-9. `07-results.tex` flat-env "~10×" noise ratio is a rounding of 0.10/0.01; the exact ratio is
8.2×.** Harmless, but since 8.2× is on record, either write "~8×" or keep "~10×" and note it is the
rounded record value. (The ~5× analog ratio is exact at 4.98× — fine.)

**SF-10. `07-results.tex` §7 intro says "four axes" then the chapter delivers five**, and omits the
headline §7.1 from the enumeration (consistency pass). *Fix:* reconcile the count and list §7.1.

**SF-11. `tab:wins` "denoiser ~30× effective" has no bridge to the body's 7.2×** (one is
effective-sample-count, the other equal-quality speedup). *Fix:* a half-sentence gloss, or harmonise
the figure. Same family: `tab:vram` caption uses GiB while the body uses MiB (pick one).

**SF-12. `04-architecture.tex` — the argmin-exactness proof should state it reproduces the *same
3σ-truncated* medium the reference uses.** The proof is valid, but the code integrates each Gaussian
only over its 3σ bound (`primitive.h`); "exactly reproduces the combined medium" is exact *for that
truncated medium*. *Fix:* add "(under the same 3σ truncation the reference applies)" once.

**SF-13. Notation — the `N` = primitive-count vs `N` = MC-sample-count collision flagged in the prior
review (§9.1) is still present** (e.g. `02-background.tex`), and Ch 6 adds a third `N` (icosphere
subdivision level). *Fix:* sample count → `spp` everywhere; icosphere level → a distinct symbol.

> SF-14…SF-18 are high-value items the parallel review (`0a76adc7`) found and I verified; the rest of
> its 24-item Should-fix list overlaps mine — read it for the remainder.

**SF-14. `03-related-work.tex` — the two closest neighbours are uncited (a committee will expect both).**
Confirmed absent by grep: **3D Gaussian Ray Tracing** (3DGRT, Moenné-Loccoz et al., SIGGRAPH Asia 2024
— OptiX ray-tracing of 3D Gaussians) and **EVER** (Exact Volumetric Ellipsoid Rendering, Mai et al.
2024). This thesis ray-traces Gaussians as a *scattering medium*; 3DGRT ray-traces them as alpha-blended
particle proxies, and EVER does exact ellipsoid volume rendering — the exact contrast that positions
this work. *Fix:* one sentence in `sec:dsyg`/`sec:radiance-fields` citing both and distinguishing DSYG's
volumetric (multiple-scattering) treatment that this thesis accelerates. (Confirm relevance before
adding, but both are squarely in-area.)

**SF-15. `07-results.tex:172` — "an order of magnitude under its cold start" overstates the record.**
Ours 0.39 s vs Mitsuba cold 2.20 s = **5.6×**, and `jit_overhead.md` itself says "≈5.5× (cold)" — not
an order of magnitude. *Fix:* "roughly half the reference's warm start and about a fifth of its cold
start" (or "5–6× under its cold start").

**SF-16. `04-architecture.tex:82` / `abstract.tex:18-20` — the back-face-cull exit mechanism may be
mis-attributed on built-in spheres (medium-confidence).** The code passes `CULL_BACK_FACING_TRIANGLES`
(`trace.cuh:37`) but the geometry is OptiX's built-in **sphere**; that triangle-named flag may be inert
on spheres, in which case the entries-only behaviour is intrinsic to the built-in sphere intersector,
not the cull flag. Culling empirically holds (worst-ray entries ≈ overlap, not ~2×), so the *outcome* is
right, but the stated *mechanism* may be imprecise. *Fix:* verify whether the flag is active on spheres;
describe the actual mechanism (one entry hit per primitive, exit closed-form) and reserve the
back-face-cull description for the icosphere/triangle variant (`anyhit.cuh:38`) where it genuinely
applies.

**SF-17. `05-validation.tex:163-172` + `fig:absorption-ladder` — tighten the single/pair provenance.**
(a) The prose's "${\sim}10^{-5}$" single-Gaussian agreement is the *converged/bias* number, but the
*figure's* displayed single-rung RMSE is ~7×10⁻⁴ (silhouette-band/MC-dominated) — 1–2 orders above the
prose, so a reader sees a mismatch. (b) The *pair* rung is validated against *Mitsuba*, not analytic
(the closed-form check is on the single rung + the collinear stress test). *Fix:* cite the converged
number for the 10⁻⁵ claim and add to the caption that the displayed RMSE is MC/edge-dominated while the
mean/bias agreement is much tighter; reword the pair rung to "matches the reference for the combined
medium to ~10⁻⁴ in the mean, with the closed-form check on the single rung and the collinear test."
(Consistent with my SF-7 evidence-tier point.)

**SF-18. `07-results.tex:125,131-132` — qualify the memory comparison.** "identical scene" / "per-ray
reservation dominates" are defensible but loose: 838 MiB is Mitsuba's GPU-wide-polled footprint for
*its own* representation vs our 578 MiB per-process peak, and the reservation is the dominant
*scene-dependent / controllable* term (fixed CUDA/OptiX context overhead is a comparable absolute share
once caps are tight). *Fix:* "on the same input geometry, each renderer's own peak footprint" and "the
per-ray reservation, not the geometry, is the dominant *scene-dependent* term." (The headline 578 < 838
is correct and reproduces; this is wording.)

---

## 3. Polish

- **P-1.** `08-conclusion.tex:22` "rigorously characterised ledger" — drop "rigorously" (filler per
  CONVENTIONS). Keep Ch 6's "most rigorously killed", which is earned.
- **P-2.** `fig:absorption-ladder` caption says "view-independently" over a single-view montage —
  soften to "(single view; view-independence shown in §5.x)".
- **P-3.** `04-architecture.tex` — `N` overloaded as the ray-crossed count where CONVENTIONS reserves
  `H`; "fast local memory" is imprecise (CUDA local memory is off-chip, fast only while L1-resident).
- **P-4.** Generalisation/coverage ratios printed to 5 digits in one place and 4 in another (Ch 7
  body vs caption) — harmonise.
- **P-5.** Add a one-line bridge between the Ch 6 estimator-ceiling cap table and the Ch 7
  measured-calibrated cap table (they intentionally differ; say so).

---

## 4. Load-bearing claims — verdicts (the seven the brief asked me to scrutinise hardest)

1. **~59× equal-quality headline — HONEST and reproduces (58.6×).** The framing is fair: §7.1 states
   up front that Mitsuba's NEE is energy-biased so analog is its *only* unbiased baseline, explicitly
   disclaims "this is not a handicap we imposed," carries the clipped-vs-raw caveat, and devotes a
   paragraph to "that 59× is specific to peaky illumination." Only gap: define the clip convention
   (SF-8).
2. **Flat-env rung — HONEST and prominent, not buried.** It is paragraphs 2–3 of the headline section,
   concedes net ~0.6× (Mitsuba ahead) and "the decisive advantage is the MIS estimator, not the bare
   sampler." Reproduces (means agree 0.002 %; analog variance ratio 4.98×). Minor: SF-3, SF-9.
3. **Scaling study — distinction correctly drawn.** Synthetic square-grid N^0.40 law and the
   production cost table are rigorously separated; the N-vs-packing-density confound is explicitly
   disclosed and the assets are "deliberately not read as a scaling curve." The thesis correctly does
   **not** assert the production N^0.71 as a law.
4. **Validation methodology — sufficient; the differential logic is sound.** The prior review's C1
   (overlap-scatter residual undisclosed) and C2 (unsound analytic-unbiasedness logic) are **both
   fixed**: the residual is disclosed in Ch 5 (`05:215-224`) and carried into Ch 8 limitations
   (`08:54-59`), magnitude-bounded ≤0.16 % peak. The furnace invariant is correctly the one
   reference-free leg that exercises the argmin sampler. Only refinement: SF-7 (separate the 10⁻³
   figure from the 10⁻⁴ bound).
5. **Memory win — correct and supported.** 578 < 838 MiB reproduces; the "per-ray local-memory
   reservation dominates, GAS negligible (0.162 KB/prim)" argument holds (SAFE-512 is a flat 1200 MiB
   across all assets). The thesis honestly concedes the *untuned* build sits above Mitsuba.
6. **Negative-results autopsy — lessons justified by the measurements.** Wavefront, footprint,
   cap-free streaming, A1, icosphere, RIS all check out against the records (table in
   `scratch/ch2-ch3-ch6.md`). The icosphere entry is honest about being a *refuted* hypothesis
   (analytic pays 1.17–1.58×), not a win. Adaptive overstates at 2× (SF-2); A1 wording is delicate
   but does not invert the premise (SF: reword `06:347`).
7. **Architecture chapter — argmin/ADT description matches a correct algorithm.** Verified
   line-for-line against `sampling.cuh`/`primitive.h`/`raygen.cuh`; both new derivations correct; MIS
   ("two shadow connections"), overflow ("detected, not corrected"), normalisation convention, and
   Condor attribution all right. Only the B-1 "sorted sequence" sentence mars it.

---

## 5. What an examiner can rely on (verified-solid)

- **Reproduction:** every headline number in §0 re-derived from banked data and PASSED.
- **Ch 4 fidelity:** single any-hit trace, entries-only collection + analytic exits, SoA hit buffer,
  bit-vector/compact-set active set, free-flight τ = −ln(1−ξ), segment-restricted inverse, two-shadow
  MIS, RR clamp, Welford, caps/overflow counter — all match the implementation. `sec:analytic-od` and
  `sec:adt` derivations correct. Three TikZ figures match the algorithm.
- **Ch 2 math:** the prior review's MINORs (1/√N, finite-variance gate, delta-tracking-estimates-T,
  balance-heuristic "best combination", mixture symbol) are **fixed**.
- **Ch 3 positioning + the "reference sorts" purge:** fixed in Ch 3; DSYG now cited as the published
  ACM TOG 2025 `@article` (doi 10.1145/3711853). (Residual sort claims survive only at the B-1 sites.)
- **Consistency:** no cross-chapter number *disagrees* — 59×, 578/838/1200 MiB, asset counts (652 /
  768 / 1024 / 25600), g = 0.85, albedo 0.9, 64 spp, +156 %/+0.4 %, 0.40 scaling all reconcile across
  every site they appear. RIS is 1.48× in Ch 6 (source) and rounded to 1.4× in abstract/Ch 8
  (conservative, consistent). The two NEE-bias numbers (+6.5 % furnace, +156 % cloud) are *different
  scenes*, not an inconsistency.
- **Honesty:** de-hyping is consistent and complete bar SF-1/B-1/B-2; the RIS win is even *rounded
  down* (1.4× vs measured 1.48×). Nine appropriately-humble framings noted in `scratch/honesty.md` as
  "do not touch."

---

## 6. Adjudicated: RIS is NOT unreachable (correcting one sub-agent's Blocking)

One pass flagged "Blocking — `--ris` not registered, the 1.48× win is unreachable at runtime."
**This is incorrect**, and I verified it against the code: `--ris` and `--ris-candidates` **are**
registered in `test/test_runner.cpp:126-127` and plumbed to the device
(`renderer_config.use_ris_` at `:178-179`/`:262-263`). The `~/winbins/exe_*` binaries are
`test_runner` builds — the binary that produced the entire campaign — so RIS was genuinely reachable
and the 1.48× was genuinely measured. Only the *separate* standalone app (`src/.../app/config.cpp`)
omits the flag. **No thesis claim is affected.**
*Optional code-hygiene (not a thesis issue):* register `--ris`/`--ris-candidates` in `config.cpp` too
so the two front-ends match; or, if any Ch 4/Ch 6 sentence implies "the renderer" = the standalone
app, scope "runtime flag" to the experiment harness. (Consistent with the standing project note:
"`test_runner` HAS `--ris`; the standalone app lacks them — only fix if that binary ships.")

---

## 7. Non-thesis housekeeping (will mislead the next review if left)

- **`results/campaign/voxgrid_DECISION.md` is stale:** its top lines read "exclude all voxel-grid",
  but the actual (and correct) decision the thesis implements is **absorption voxel-GT IN, scattering
  voxel-GT OUT**. `sec:voxel-gt` is rightly present. Update the doc's summary so it stops contradicting
  the chapter.
- **`config.cpp` vs `test_runner.cpp` flag drift** (see §6) — optional.

---

## Appendix — per-pass detail (scratch files)

- `scratch/ch4-arch.md` — Ch 4 vs code; derivations verified correct.
- `scratch/ch5-validation.md` — validation logic; C1/C2/M1–M4 confirmed fixed.
- `scratch/ch7-ch8-results.md` — every number vs record + arithmetic; framing verdicts.
- `scratch/ch2-ch3-ch6.md` — background/related/optimization; autopsy-justified table; defense Qs.
- `scratch/honesty.md` — whole-thesis overclaim sweep + "do not touch" list.
- `scratch/consistency-figures-writing.md` — number table, per-figure table, build status.
- `scratch/reproduction.md` — the §0 numbers, full method, and the two evidence-tier analysis.

**Parallel review (independent, same day):** `thesis/reviews/2026-06-15-0a76adc7.md` — 26-agent
adversarial pass; source of B-3/B-4/B-5 and SF-14…SF-18 (all re-verified here). Its scorecard
reproduces the same headline numbers as §0. Read it for its full 24-item Should-fix list (the items not
folded in here overlap this doc's Should-fixes).
```
