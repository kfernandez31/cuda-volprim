\chapter{Performance Engineering and Optimization}
\label{ch:optimization}

The renderer validated in \Cref{ch:validation} was, at first, slower than the Mitsuba reference it
matched: on the scattering cloud it ran roughly $1.9\times$ slower per sample \emph{and} $2.9\times$
noisier per sample---about $5\times$ at equal quality. Closing that gap, and then overtaking the
reference, is the subject of this chapter. The work is presented not as a list of speedups but as a
\emph{method}: every candidate is classified by how it can honestly be justified, and only those whose
outcome is genuinely informative are run as controlled ablations. Equally important is what did
\emph{not} work---a ledger of dead ends (\Cref{sec:autopsies}) that together characterise the
performance ceiling of this class of renderer and are themselves a contribution.

\section{The starting gap and the bottleneck}
\label{sec:bottleneck}

Optimisation began with profiling, not guessing. NSight Compute locates the bottleneck firmly in
\emph{memory latency}: the kernel is latency-bound on scattered global loads of the per-primitive
\texttt{Primitive} struct, at roughly $22\%$ occupancy, itself capped by the code-intrinsic register
pressure of the erf and phase-function chains. The primitive data is already $82$--$98\%$
cache-resident, so reducing its \emph{footprint} cannot help much---the cost is in the latency of
gathering scattered primitives, not in bandwidth. The structural reading of this is the same one that
shaped the architecture (\Cref{sec:gpu-impl}): the per-bounce arithmetic is cheap but the per-ray state
is large and persistent, so the algorithm is \emph{megakernel-shaped}. This single fact predicts
several of the null results in \Cref{sec:autopsies}.

\section{A taxonomy of optimisations}
\label{sec:four-modes}

Whether to \emph{measure} an optimisation is itself a decision. A controlled on/off ablation is worth
running only when its outcome is not a foregone conclusion; otherwise the honest justification is a
complexity argument, a citation to standard practice, or a statement of why no fair experiment is
possible. Each optimisation in this chapter is therefore placed in one of four modes:

\begin{table}[htp]
  \centering
  \begin{tabular}{@{}lll@{}}
    \toprule
    Mode & When it applies & Examples \\
    \midrule
    \textbf{(M) Measured} & genuine choice, non-obvious outcome & RIS, RR depth, shell geometry, autopsies \\
    \textbf{(C) Complexity-argued} & naive baseline $\to$ obvious form & scan fixes, argmin, $O(1)$ sets, compact buffer \\
    \textbf{(S) Standard practice} & textbook; cite, don't measure & multi-stream, FMA, coalescing, fast-math \\
    \textbf{(I) Infeasible} & no valid A/B possible & (emptied --- see \Cref{sec:icosphere}) \\
    \bottomrule
  \end{tabular}
  \caption{The four justification modes. The chapter measures what is informative, complexity-argues
  what was merely made-correct, cites the standard, and states plainly what could not be tested.}
  \label{tab:four-modes}
\end{table}

\section{Measured wins}
\label{sec:wins}

The kept speedups, each a controlled ablation against the validated baseline, are summarised in
\Cref{tab:wins}. The single largest was eliminating a redundant per-bounce $O(N)$ containment scan;
the most dramatic in relative terms was a smarter shadow-ray transmittance evaluation. Russian-roulette
depth, a free efficiency knob, was tuned from $5$ to $12$ (\Cref{fig:rr-depth}). After these, the
equal-quality comparison against Mitsuba inverts: the renderer reaches the same noise level in less time
(\Cref{ch:results}).

\begin{table}[htp]
  \centering
  \begin{tabular}{@{}llc@{}}
    \toprule
    Optimisation & Effect & FINDINGS \\
    \midrule
    Shadow-ray transmittance         & $\sim$\,12--15$\times$ (shadow kernel) & §8.16 \\
    Skip per-bounce containment scan & $\sim$\,16\% frame                     & §8.23 \\
    Russian-roulette depth $5\to12$  & $\sim$\,11\%                           & §8.33 \\
    Dedup bounce-0 scan              & $\sim$\,8\%                            & §8.19 \\
    Any-hit transmittance fusion     & $\sim$\,3\%                            & §8.18 \\
    Fast \texttt{erf} in the hot path & $\sim$\,1.5\%                          & §8.21 \\
    OptiX denoiser (showcase)        & $\sim$\,30$\times$ effective           & §8.22 \\
    \bottomrule
  \end{tabular}
  \caption{Measured (M-mode) optimisations kept in the renderer. Each is a controlled on/off ablation;
  effects are on the scattering cloud at the fixed operating point.}
  \label{tab:wins}
\end{table}

\begin{figure}[htp]
  \centering
  \includegraphics[width=0.8\linewidth]{figures/rr_depth.pdf}
  \caption{Russian-roulette efficiency on the scattering cloud: frame time against the RR start depth at
  matched quality. The chosen depth ($12$) sits at the efficiency knee; deeper paths buy negligible
  variance reduction for added cost.}
  \label{fig:rr-depth}
\end{figure}

\section{The algorithmic highlight: volumetric product-RIS}
\label{sec:ris}

The one \emph{studied} algorithmic win is in direct lighting. The baseline next-event estimator spends
one shadow ray per scattering vertex on an environment sample drawn by luminance. Resampled importance
sampling~\cite{Talbot2005} instead draws $K$ environment candidates, resamples one in proportion to a
cheap target (phase value $\times$ environment luminance) via a weighted reservoir~\cite{Bitterli2020},
and traces a shadow ray only for the survivor---paying $K$ cheap evaluations to make the single
expensive shadow ray count. With $K=6$ this gives about $1.4\times$ at equal quality on
environment-map scenes. Crucially it is \emph{scene-dependent}: on a flat (constant) environment, where
the candidates are interchangeable, the resampling overhead makes it $\sim\!2.5\times$ \emph{worse}. It
is therefore shipped behind a runtime flag (default off, plain MIS), not as an unconditional change
(\Cref{fig:ris-ksweep}).
The result was validated unbiased against the Mitsuba-analog ground truth on a constant environment
(furnace-clean), and---being a single-strategy estimator---it surfaced and forced the fix of a latent
environment-sampling bug (a texel-centre quantisation that the prior MIS had masked).

\begin{figure}[htp]
  \centering
  \includegraphics[width=0.8\linewidth]{figures/ris_ksweep.pdf}
  \caption{Volumetric product-RIS versus plain MIS at equal quality, as the candidate count $K$ varies.
  On an environment map RIS wins (peaking near $K=6$); on a flat environment the resampling overhead
  makes it a net loss---hence the runtime flag.}
  \label{fig:ris-ksweep}
\end{figure}

\section{Complexity-argued improvements}
\label{sec:complexity}

Several changes are not ablations because their ``before'' was a naive or accidental baseline, not a
defended design; regressing to it would only re-derive that doing things the slow way is slow. They are
justified by complexity (\Cref{tab:complexity}) and reported with whatever incidental speedup they
brought. The most consequential is the architecture's own argmin scatter sampler (\Cref{sec:adt}),
which removes the reference's per-bounce sort and root-find outright.

\begin{table}[htp]
  \centering
  \begin{tabular}{@{}lll@{}}
    \toprule
    Step & Before & After \\
    \midrule
    Per scatter bounce       & $O(N + A^2)$              & $O(N + A)$ \\
    Boundary sort (scatter)  & $O(H^2)$ / $O(H\log^2 H)$ & eliminated \\
    Scatter distance         & bisection / Newton       & closed-form $\operatorname{erf}^{-1}$ \\
    Active-set insert/test   & $O(\log A)$               & $O(1)$ (bit vector) \\
    Hit record               & 20\,B (unified)          & 6\,B (structure-of-arrays) \\
    \bottomrule
  \end{tabular}
  \caption{Complexity-argued (C-mode) improvements. The argmin sampler and the data-structure choices
  reduce per-bounce work asymptotically; these are justified by the bound, not by a regression ablation.}
  \label{tab:complexity}
\end{table}

\section{Reasoned decisions, not ablated}
\label{sec:reasoned}

Some choices are deliberately \emph{not} measured. Standard-practice optimisations---multiple CUDA
streams with asynchronous transfers, fused multiply-adds and loop unrolling, \texttt{float4}-coalesced
buffers, the fast-math flag set, GAS compaction and \texttt{PREFER\_FAST\_TRACE}---are cited rather than
ablated, because regressing to ``everything on one stream'' would re-derive a textbook result. One
choice originally filed here as \emph{infeasible}---analytic versus tessellated sphere geometry, whose
tessellated half predated the current architecture---turned out to be measurable after all: porting
only the historical icosphere mesh into the current renderer behind a build switch yields an A/B in
which the per-primitive geometry is the \emph{only} difference. \Cref{sec:icosphere} reports that
experiment; its outcome refutes the expectation this section once recorded.

\section{Analytic versus tessellated primitive geometry}
\label{sec:icosphere}

The reference implementation faced this exact choice and went the other way: its authors first
implemented a custom ray-ellipsoid intersection program in OptiX, found it ``significantly less
performant than a hardware-accelerated ray-triangle intersection test'', and switched to tessellated
icospheres---duplicated per primitive rather than instanced---reporting an average $4.96\times$
speedup, with their highest-tessellated shell (a \num{320}-triangle icosphere) performing
best~\cite{DSYG}. This
renderer instead uses OptiX's built-in analytic sphere over a single instanced unit GAS. The A/B ports
the historical icosphere mesh (subdivision $N$: \num{20}, \num{80}, \num{320}, \num{1280} triangles)
into the current renderer behind a compile switch; the IAS instancing, the any-hit entry collection,
and the analytic optical-depth integration are untouched, with one addition---a convex shell is
entered through a front face and left through a back face, so the any-hit keeps only front-face hits,
preserving the one-entry-per-primitive contract the sphere provides natively.

\begin{table}[htp]
  \centering
  \begin{tabular}{@{}lrrrrrr@{}}
    \toprule
    & & \multicolumn{2}{c}{frame time (cloud)} & \multicolumn{3}{c}{RMSE vs analytic} \\
    \cmidrule(lr){3-4} \cmidrule(l){5-7}
    Shell & tris & abs. & rel. & cloud & tornado & bunny \\
    \midrule
    analytic sphere & --- & \SI{15.52}{\second} & $1.00\times$ & 0 & 0 & 0 \\
    icosphere $N{=}0$ & 20   & \SI{9.80}{\second}  & $0.63\times$ & $4.0\times10^{-2}$ & $9.4\times10^{-3}$ & $1.2\times10^{-2}$ \\
    icosphere $N{=}1$ & 80   & \SI{11.22}{\second} & $0.72\times$ & $1.0\times10^{-2}$ & $1.9\times10^{-3}$ & $2.5\times10^{-3}$ \\
    icosphere $N{=}2$ & 320  & \SI{11.99}{\second} & $0.77\times$ & $2.5\times10^{-3}$ & $4.6\times10^{-4}$ & $5.8\times10^{-4}$ \\
    icosphere $N{=}3$ & 1280 & \SI{13.23}{\second} & $0.85\times$ & $8.2\times10^{-3}$ & $4.6\times10^{-3}$ & $4.4\times10^{-3}$ \\
    \bottomrule
  \end{tabular}
  \caption{Analytic built-in sphere versus tessellated icosphere, with only the per-primitive geometry
  differing. Frame times: cloud scattering under the environment-lit showcase, \num{128} spp, median of
  five interleaved rounds at locked clocks (within-arm spread ${\le}1.5\,\%$). RMSE: converged
  absorption renders against the analytic render, which is exact here---the instanced primitive
  \emph{is} a sphere---so the residual is pure tessellation error.}
  \label{tab:icosphere}
\end{table}

The outcome (\Cref{tab:icosphere}) refutes the hypothesis under which the analytic sphere was chosen.
The tessellated shell is \emph{faster at every tessellation level}: the analytic sphere pays
$1.17$--$1.58\times$ in frame time for its exactness. The mechanism is the one the reference named:
on RTX-class hardware, ray-triangle intersection executes on dedicated ray-tracing cores, whereas the
``built-in'' sphere primitive is a software intersection module on the traversal path. The magnitude
here is smaller than the reference's $4.96\times$ because the baselines differ---their analytic arm
was a custom ellipsoid intersector over per-primitive world-space bounds, where anisotropic primitives
inflate axis-aligned boxes, while this renderer's analytic arm already benefits from the instanced
unit-sphere GAS with per-instance transforms.

Accuracy tells the opposite story, and a non-monotone one. From $N{=}0$ to $N{=}2$ the error falls
${\sim}4\times$ per subdivision with the physically expected sign (the inscribed shell is slightly
smaller than the true $3\sigma$ ellipsoid, so renders are marginally too bright). At $N{=}3$ the
error \emph{rises} on all three assets and flips sign: a localised population of sliver triangles
mis-places entry points for near-grazing rays, and the count of affected pixels tracks per-primitive
screen footprint rather than anisotropy (the round-primitive cloud is hit hardest). Past $N{\approx}2$,
finer tessellation buys worse accuracy at higher cost---consistent with the reference never going
beyond \num{320} triangles.

The decision, then, is a genuine accuracy-versus-performance trade, and this renderer keeps the
analytic sphere \emph{for correctness, at a now-quantified price}. The faceting bias at the best
tessellation ($N{=}2$, ${\sim}0.16\,\%$ systematic brightening on the cloud) is invisible in an image
but is an order of magnitude above the $10^{-4}$-level energy agreement that the validation gates of
\Cref{ch:validation} hold the renderer to; the analytic shell is what makes those gates attainable.
Where raw throughput matters more than validation-grade exactness, the measured frontier says
$N{=}2$---matching the reference's choice of a \num{320}-triangle icosphere---is the right shell, and
the instanced single-GAS design keeps even that choice memory-trivial (\SI{9.6}{\kilo\byte} compacted,
versus per-primitive duplication in the reference).

\section{Negative results: an autopsy ledger}
\label{sec:autopsies}

The following ideas were implemented or seriously evaluated and did not pay off. Each is reported as
hypothesis, cheap kill-test, measurement, and lesson---collectively a map of where effort on this
renderer does \emph{not} go.

\begin{itemize}
  \item \textbf{Wavefront refactor (§8.34) --- the big one.} Splitting the megakernel into per-stage
  kernels was $100$--$1400\times$ \emph{slower}: the ${\sim}352$\,B/ray state must stream through DRAM
  every bounce and goes super-linear once the working set exceeds L2. \emph{Lesson:} moving persistent
  per-ray state to global memory is fatal for this workload---it is megakernel-shaped, and this rules
  out a whole family of designs (wavefront, ray-sorting, neural caches).
  \item \textbf{Megakernel footprint reduction (§8.29).} SoA layouts, field reordering, and
  \texttt{\_\_ldg} hints were null: the data is already cache-resident, so the cost is load
  \emph{latency}, not footprint.
  \item \textbf{Adaptive sampling (§8.30).} Per-pixel convergence stopping was a net loss
  ($\sim\!2\times$ slower at equal quality, plus firefly bias) on the high-variance cloud; kept,
  default-off.
  \item \textbf{Per-step Rao--Blackwellisation, ``A1'' (§8.27, \Cref{app:a1}).} A direct/indirect
  variance trade-off that helps only on flat environments, where the renderer already wins; no net gain.
  \item \textbf{Density-contribution culling (§8.31)} --- redundant with the BVH's $3\sigma$ bound.
  \textbf{Track-length combine (§8.32)} --- unnecessary, the environment transmittance is already
  analytic. \textbf{Exit-caching (§8.35)} --- bit-identical, no win (the extra local state hit the
  occupancy wall). \textbf{Env-IS alias table (§8.36)} --- correct but sub-$1\%$; environment sampling
  is not the bottleneck. \textbf{Owen--Sobol sampling (§8.20)} --- no win on this high-variance
  integrand.
  \item \textbf{Path guiding (§8.38) --- deferred, not failed.} No \emph{cheap} kill-test could
  validly predict its gain (proxies measure the NEE-handled layer, not the multi-bounce tail), the
  upside is bounded low by the near-diffuse deep field, and the cost is downside-skewed; building it
  would have been a multi-day bet against weak evidence.
\end{itemize}

The unifying lesson is the bottleneck of \Cref{sec:bottleneck}: at low occupancy bound by global-load
latency, footprint and cache micro-optimisations are near-null, and any restructuring that externalises
per-ray state backfires. The one true hardware lever for this divergence---OptiX Shader Execution
Reordering~\cite{NvidiaSER2022}---is Ada-only and therefore unavailable on the Ampere RTX~3090 used here.

\section{Memory}
\label{sec:opt-memory}

Performance is not only time, and the memory story has one result that matters: the megakernel's
per-ray state. The wavefront autopsy above is fundamentally a \emph{memory} story---its ${\sim}352$\,B
of per-ray state, multiplied by the live ray count, is what overruns L2 and collapses performance, the
clearest evidence that the megakernel's register-resident state is not an incidental choice but the
load-bearing one. The acceleration structure itself is not where the budget goes: it is sub-megabyte
for the cloud and a few megabytes for the bunny, built with compaction as a matter of course (a single
build flag, \texttt{OPTIX\_BUILD\_FLAG\_ALLOW\_COMPACTION}, also used by the reference) and dwarfed by
the per-ray state.

\paragraph{Per-asset capacity budgets.} The per-ray buffers are bounded by the worst-case primitive
overlap a scene presents, which the offline estimator of \Cref{sec:gpu-impl} predicts from geometry
alone. \Cref{tab:overlap} reports that estimate across assets. The overlap varies by more than
$25\times$ and---crucially---does not track the primitive count: a \num{4096}-primitive WDAS cloud fit
demands an order of magnitude more capacity than a \num{24576}-primitive one, because overlap is set by
how densely the Gaussians pack, not how many there are. No single static cap fits every scene: the
\num{128}-entry buffers suit the primary cloud, but the denser fits and the bunny would overflow them
and require a larger compile-time cap (which the estimator sizes ahead of the build).

\begin{table}[htp]
  \centering
  \begin{tabular}{@{}lrrr@{}}
    \toprule
    Asset & Primitives & Max point overlap & Max ray entries \\
    \midrule
    Disney cloud (primary) & \num{652}   & \num{45}  & \num{96}  \\
    WDAS cloud fit         & \num{768}   & \num{19}  & \num{108} \\
    WDAS cloud fit         & \num{4096}  & \num{529} & \num{706} \\
    WDAS cloud fit         & \num{24576} & \num{57}  & \num{437} \\
    Stanford bunny         & \num{25600} & \num{245} & \num{387} \\
    \bottomrule
  \end{tabular}
  \caption{Worst-case $3\sigma$ overlap per asset, predicted offline by the cap estimator (point
  overlap sizes \texttt{MAX\_ACTIVE\_PRIMS}; ray entries size \texttt{HIT\_BUFFER\_CAPACITY}). Overlap
  spans more than $25\times$ and is independent of primitive count, so the caps are necessarily
  asset-dependent. The three WDAS rows are distinct density fits of the Disney cloud.}
  \label{tab:overlap}
\end{table}
