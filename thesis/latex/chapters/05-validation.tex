\chapter{Validation Methodology and Correctness}
\label{ch:validation}

A renderer that introduces a new sampling scheme (\Cref{ch:architecture}) must earn trust: the
sort-free analog-decomposition sampler is only useful if it produces the \emph{same} images a correct
volumetric path tracer would, and the production optimisations of \Cref{ch:optimization} are only
admissible if they do not perturb that result. This chapter establishes that correctness. It first
explains \emph{how} the renderer is validated---a differential methodology forced by the absence of an
absolute ground truth (\Cref{sec:reference-problem})---and the statistics needed to compare two
stochastic renderers fairly (\Cref{sec:compare-correctly}). It then walks a ladder of increasingly
complex scenes, from a single Gaussian with a closed-form answer up to the full cloud, in absorption
(\Cref{sec:absorption-ladder}) and then in scattering (\Cref{sec:scattering-ladder}), validates each
rendering feature against the reference (\Cref{sec:feature-validation}), and closes on the combined
showcase (\Cref{sec:money-shot}). Throughout, the scattering ladder and the furnace
invariant serve as the empirical \emph{unbiasedness evidence} for the analog-decomposition sampler: a
biased free-flight rule would shift the converged scattering result away from the energy-conserving
furnace value and from the independently-correct Mitsuba-analog reference.

\section{Experimental setup}
\label{sec:setup}

All results in this thesis were measured on a single workstation---an NVIDIA GeForce RTX~3090 (Ampere,
compute capability~8.6, \SI{24}{\giga\byte})---with one explicitly-scoped exception noted below. The
renderer is built on CUDA and OptiX~9 in its release configuration (\texttt{-O3},
\texttt{-{}-use\_fast\_math}). The Mitsuba~3 reference runs on the same GPU through its CUDA backend, so
cross-renderer comparisons are on identical hardware. The lone exception is the Shader Execution
Reordering probe of \Cref{sec:ser}, which requires Ada-generation hardware and was therefore run on a
rented NVIDIA GeForce RTX~4090 (Ada, compute capability~8.9; CUDA~13.2, driver~595.58); those figures are
marked throughout as a cross-architecture probe and feed into no 3090 result. Unless noted
otherwise, every timing figure is taken at a single fixed operating point---the card's sustained full
clock---to avoid clock-throttling artefacts (\Cref{sec:compare-correctly}); the equal-quality metric,
being a ratio, is robust to the operating point regardless. Every timing A/B additionally
\emph{interleaves} its two arms within each seed block (arm~A, arm~B, arm~A, \ldots) rather than running
one arm fully and then the other, so that any residual clock or thermal drift affects both arms equally
and cancels in the comparison.

\section{Differential validation}
\label{sec:reference-problem}

No absolute, per-asset ground truth is available for these Gaussian assets, so correctness is
established \emph{differentially}---by quantitative comparison against a trusted reference renderer,
backed by analytic ground truth wherever a scene admits a closed form.

The renderer is compared against Mitsuba~3~\cite{Mitsuba3}---configured identically,
primitive-for-primitive, from the same Gaussian data---and the difference is quantified. Mitsuba is
mature, widely trusted, and runs the same physically based volumetric path tracing, so a small,
converging difference is strong evidence of correctness, while a structured difference localises a
bug. This turns an unfalsifiable ``does it look right?'' into a measurable gap, and it is the backbone
of every result below. Where an even stronger check exists---a scene with a closed-form answer---the
renderer is held to that analytic ground truth directly.

\paragraph{A reference is only as good as its configuration.} A reference image carries no meaning
without the exact configuration that produced it---integrator, density scale, and lighting. The
distinction that matters most is the \emph{integrator}: the optimisation-pipeline renders shipped
alongside each asset (\texttt{reference\_pyrN} and \texttt{optimized\_pyrN}) come from a
\emph{tomographic} line integral, not physically based path tracing. On the identical cloud from the
identical camera, that shipped tomographic reference reads a mean radiance of $0.53$ against this
renderer's path-traced $0.31$---roughly $1.7\times$ apart \emph{purely} from the convention difference,
before scattering or any $\sigma$-scale choice enters. The shipped renders are therefore \emph{not}
valid targets for this renderer: they depict the fit's target volume and the fitted primitives in the
optimiser's own tomographic convention, not a path-traced image under our configuration. Chasing them by
tuning the density scale silently conflates an integrator mismatch, a convention mismatch, and---for the
target render---the Gaussian \emph{fit} error, which is not the renderer's to absorb. Every comparison in
this chapter is therefore made only against references regenerated under the renderer's own configuration
(matched primitive-for-primitive: same integrator, $\sigma$-scale, camera, and lighting); the agreement
those achieve is meaningful precisely because the configuration is controlled.

\subsection{An independent integrator: a voxel-grid cross-check}
\label{sec:voxel-gt}

The Mitsuba comparison shares one assumption with this renderer: both integrate the medium as a sum of
analytic Gaussian primitives, so a systematic error in that shared model---a wrong normalisation, a
mis-stated kernel---could in principle survive in both. A stronger check breaks the shared assumption
entirely. The Gaussian field is baked into a dense voxel grid in the renderer's own, mass-conserving
density convention,
\[
  \sigma_t(\mathbf{x})=\sum_i M_i\,(2\pi)^{-3/2}\!\prod_j s_{ij}^{-1}\;
  \exp\!\big(-\tfrac12\lVert S_i^{-1}R_i^{\!\top}(\mathbf{x}-\boldsymbol\mu_i)\rVert^2\big),
\]
and that grid is rendered by Mitsuba's \texttt{heterogeneous}/\texttt{gridvolume} path tracer via delta
tracking---a method that shares no code and no Gaussian mathematics with either analytic renderer.

In \emph{absorption}---where the image depends only on transmittance, with no scattering variance---the
cross-check is clean (\Cref{fig:voxel-gt}). The grid render matches the analytic render exactly through
the opaque interior and the empty background---the median pixel difference is zero---and the only
residual is a thin band on the silhouette, where the grid blurs the sub-voxel-sharp Gaussian boundary
the analytic form resolves crisply. That residual shrinks monotonically as the grid is refined (from
$128^3$ to $600^3$: RMSE $0.072\!\to\!0.057$, mean $0.394\!\to\!0.401$ toward the analytic $0.416$),
confirming the analytic render is the limit of the grid renders as discretisation vanishes; more samples
per pixel do not move it, so the residual is discretisation, not noise. The cross-check thus both
corroborates the renderer and quantifies the edge fidelity the analytic primitive buys over a voxelised
proxy. (Scattering is deliberately not cross-checked this way: a dense-grid scattering reference for this
high-dynamic-range medium is either block-biased, with a coarse local majorant, or firefly-limited, with
an exact global one, so scattering correctness rests on the furnace invariant of
\Cref{sec:scattering-ladder} and the Mitsuba-analog comparison.)

\begin{figure}[htp]
  \centering
  \includegraphics[width=\linewidth]{figures/voxel_gt.pdf}
  \caption{Independent voxel-grid cross-check (cloud, absorption, same camera). \emph{Left:} the
  analytic Gaussian renderer. \emph{Centre:} the same density field baked to a $600^3$ grid and rendered
  by Mitsuba's dense-grid delta-tracking path tracer---no shared code or Gaussian mathematics.
  \emph{Right:} $\lvert\Delta\rvert$ ($\times5$). Interior and background match exactly (median pixel
  difference zero); the only residual is the silhouette band where the grid blurs the sharp Gaussian
  boundary, and it shrinks monotonically with grid resolution.}
  \label{fig:voxel-gt}
\end{figure}

\section{How to compare correctly}
\label{sec:compare-correctly}

Comparing two Monte~Carlo renderers is itself error-prone, and several pitfalls had to be controlled.
\emph{Bias and variance must be separated.} A single per-pixel RMSE between two noisy renders conflates
estimator \emph{bias} (a systematic difference that survives convergence) with \emph{variance} (noise
that vanishes as samples grow). Concretely, for two independent renders $A$ and $B$ of the same scene,
the expected per-pixel squared difference splits cleanly into the two,
\[
  \mathbb{E}\big[(A-B)^2\big]
  = \underbrace{\big(\mathbb{E}[A]-\mathbb{E}[B]\big)^2}_{\text{bias}^2}
  + \underbrace{\operatorname{Var}[A]+\operatorname{Var}[B]}_{\text{noise}},
\]
so a single raw difference cannot, on its own, tell a genuine discrepancy from sampling noise.
Re-rendering each image under several independent seeds estimates the noise term directly (as the
inter-seed variance); when the measured difference is statistically consistent with that noise floor,
the bias term is zero to within the noise---the precise sense in which two renders are said here to
``match to within Monte~Carlo noise.'' The renderer is therefore checked for bias by comparing
\emph{converged} means---or, where available, against an analytic value---while \emph{efficiency} is
compared separately and at \emph{equal quality}: since Monte~Carlo error falls as
$O(1/\sqrt{N})$ (\Cref{sec:monte-carlo}), the meaningful quantity is the noise constant
$k = \mathrm{RMSE}^2 \cdot N$ (equivalently, inter-seed variance $\times$ spp---the two coincide when
both estimators are unbiased, as they are here, and the headline figures of \Cref{ch:results} use the
variance form), and one renderer is faster than another only if it reaches the same $k$ in less time. Raw frame-time comparisons at fixed sample count are reported only alongside this
equal-quality figure (\Cref{ch:results}).

Two further controls matter. \emph{Fireflies}---rare, very bright samples---inflate RMSE out of
proportion to perceived error, so robust statistics (high percentiles, clipped means) accompany the raw
numbers, and the difference in firefly behaviour between the renderers is itself a result
(\Cref{ch:results}). And all timing is reported at a single, fixed GPU operating point, because clock
throttling otherwise injects spurious variation; the equal-quality metric, being a ratio, is robust to
it regardless.

\section{Absorption: the correctness ladder}
\label{sec:absorption-ladder}

With scattering disabled (albedo $0$), the image depends only on transmittance, which for a Gaussian
mixture is the closed-form optical depth of \Cref{sec:analytic-od}---so the ground truth is
\emph{analytic}, independent of any reference renderer. This isolates the geometry and the optical-depth
integration from the stochastic scattering logic.

\paragraph{Single Gaussian.} A single primitive integrates to a closed-form transmittance at every
pixel. Against the renderer's own analytic self-check the converged agreement is $\sim\!2\times10^{-5}$,
confirming the ray--primitive transform, the erf-based optical depth, and the camera model. The montage
in \Cref{fig:absorption-ladder} instead compares against an independent reimplementation of the closed
form, which carries a small one-sided offset (ratio $1.0007$, RMSE $\sim\!7\times10^{-4}$, present even
in the near-transparent background)---an antialiasing and exposure mismatch in that reimplementation,
not a transport error in the renderer.

\paragraph{Overlapping primitives.} The next rung stacks primitives so their Gaussians overlap, testing
two things the single-Gaussian rung cannot: that the optical depths of overlapping primitives \emph{add}
correctly along a ray, and that the active-set bookkeeping the argmin sampler relies on
(\Cref{sec:adt})---which primitives a point lies inside---is maintained correctly. Up to the active-set
capacity the renderer matches the reference for the combined medium to $\sim\!10^{-4}$ in the mean; the
closed-form check on additive optical depth is carried by the single-Gaussian rung and the collinear
stress test below, not by this plotted pair (whose banked reference is Mitsuba).
Pushed past that capacity by a deliberately degenerate \emph{collinear} stress test---$m$ identical
Gaussians stacked so the exact answer is a single
$m$-fold-denser Gaussian---it exposed, and then confirmed the fix for, the cap-overflow behaviour discussed
in \Cref{sec:gpu-impl}: past the old capacity the active set silently dropped primitives, producing a
predictable under-absorption that matched $\exp(-\min(\text{cap},m)/m \cdot \tau_0)$ to $10^{-4}$. The
same stress test surfaced a \emph{reference} limitation---Mitsuba's volumetric primitive integrator
itself under-absorbs as identical primitives are stacked---a reminder that ``differential'' cuts both
ways and that the analytic check is the final arbiter.

\paragraph{The cloud.} The top rung is the full \num{652}-primitive Disney cloud (the headline asset
throughout this thesis: Gaussians fit to the Disney/WDAS cloud volume~\cite{WDASCloud2017}). It matches the reference across
all $24$ camera views---against Mitsuba, with a float64 brute-force analytic transmittance adjudicating
the edge band on a representative view---establishing absorption correctness at scene scale before
scattering, and hence multiple sampled bounces, was introduced. The absorption ladder is therefore
three rungs---single Gaussian (analytic), overlapping primitives (reference, with additivity checked
analytically via the collinear test), full cloud (reference + analytic edge-band spot-check)---each
adding one source of complexity over the last;
\Cref{fig:absorption-ladder} collects them.

\begin{figure}[htp]
  \centering
  \includegraphics[width=\linewidth]{figures/absorption_ladder.pdf}
  \caption{Absorption-only validation up the complexity ladder: a single Gaussian, an overlapping pair,
  and the full \num{652}-primitive cloud. The single Gaussian is held to its closed-form transmittance
  (line-integrated over the renderer's $3\sigma$ kernel support, so the comparison isolates numerical
  agreement rather than the finite-extent truncation both share);
  the pair and cloud are compared against Mitsuba (additivity checked analytically via the collinear
  test, and the cloud additionally against a float64 analytic edge-band spot-check across its \num{24}
  views). Each rung is annotated with the converged-mean ratio and the per-pixel RMSE; the RMSE carries
  the montage's Monte Carlo noise and a small reimplementation offset and so sits above the mean
  agreement (the single Gaussian's converged agreement against the renderer's own check is
  $\sim\!2\times10^{-5}$).}
  \label{fig:absorption-ladder}
\end{figure}

\section{Scattering validation}
\label{sec:scattering-ladder}

Enabling scattering (albedo $>0$) activates the full path tracer: the analog-decomposition sampler, the
phase function, and next-event estimation over many bounces. There is no closed-form image here, so two
checks are used: a reference-free energy invariant, and the differential comparison against Mitsuba.

\paragraph{Furnace test.}\label{par:furnace} In a uniform environment with albedo $1$ and no absorption,
energy conservation demands that every pixel return exactly the background---any deviation is estimator
bias, with no reference render, asset, or density scale entering. The renderer passes this furnace test
flat to within noise, confirming the scattering loop conserves energy, and Mitsuba's \emph{analog} mode
passes it likewise. Mitsuba's \emph{next-event} integrator, by contrast, over-counts in-scattered light
at the Gaussian's centre, depth-invariantly (identical at path depths \num{32} and \num{256}, so not a
truncation artefact); because the furnace removes every setup variable, this establishes the over-count
as intrinsic to that estimator rather than an artefact of the asset, the density scale, or this renderer
(the mechanism, and how it grows with optical thickness and overlap to \num{+156}\,\% on the dense cloud,
are diagnosed in \Cref{sec:results-firefly}). Its \emph{analog} (NEE-disabled) mode---which is
unbiased---is therefore used as the scattering ground truth throughout.

\paragraph{The scattering ladder.} Against Mitsuba-analog the renderer is validated up the same ladder:
a single scattering Gaussian, clusters, and the full cloud, each at albedo $0.9$. The single rung
matches to within noise; the cluster and cloud rungs match in the bulk but leave one small,
convergence-stable residual in the \emph{overlap} region---a $+2\times10^{-4}$ core difference on the
cluster (about six standard errors), echoed as ${+}1\times10^{-4}$ in the densest part of the cloud.
It is below $0.2\%$ of the peak and confined to vertices where many primitives overlap. It is
\emph{not} a single-precision inversion artefact: replacing the float $\operatorname{erf}^{-1}$ with a
double-precision one moves the converged cluster mean by ${\sim}2\times10^{-8}$ at the overlap---four
orders of magnitude below the residual---so the cause is an overlap-regime effect, either the argmin's
free-flight \emph{independence} assumption under heavy overlap (\Cref{sec:adt}) or next-event shadow-ray
transmittance evaluated from a vertex \emph{inside} overlapping primitives. Bounded and
convergence-stable, it is carried as a characterised residual (\Cref{ch:conclusion}). The agreement everywhere else is the empirical evidence that
the sampler is unbiased to within ${\sim}10^{-4}$: a grossly wrong free-flight distribution would
shift the converged mean far more, and it does not (\Cref{fig:scattering-ladder}). That the
methodology resolves a $10^{-4}$ effect at all is itself a measure of its sensitivity.

\begin{figure}[htp]
  \centering
  \includegraphics[width=\linewidth]{figures/scattering_ladder.pdf}
  \caption{Scattering unbiasedness on the full cloud (meadow, albedo $0.9$, $g=0.85$): as independent
  \num{64}-spp seeds accumulate, the running mean of this renderer (MIS) and of the Mitsuba-analog
  reference converge to the same value ($0.3214$ and $0.3201$, agreeing to \num{0.4}\,\%, both unbiased).
  Ours converges from the first seed; the firefly-prone analog approaches it only gradually. The faint dots are the
  per-seed estimates being averaged---ours forms a tight band while the analog scatters ${\sim}68\times$
  wider, so its mean-estimate standard error is ${\sim}68\times$ larger at equal sample count. The
  single-Gaussian and cluster rungs agree the same way in the mean; their per-pixel images are
  uninformative because scattering under a near-constant environment is almost invisible and the analog
  reference is firefly-limited.}
  \label{fig:scattering-ladder}
\end{figure}

\section{Feature validation}
\label{sec:feature-validation}

Each rendering feature is validated against the reference before it is trusted in combination:
environment-map illumination, the Henyey--Greenstein phase function (forward scattering, $g = 0.85$),
multiple importance sampling, and per-channel coloured albedo---a medium that scatters a different
fraction of light per RGB channel (a tinted, rather than grey, albedo). Each matches the reference to
within Monte~Carlo noise, with one exception: coloured albedo leaves a small channel-dependent residual.
It is the only \emph{feature-validation} exception; together with the overlap-region scattering residual
of \Cref{sec:scattering-ladder} and a ${+}1.8\times10^{-4}$ brightness systematic in low-density interiors, it is one of three small
residuals documented as known limitations (\Cref{ch:conclusion}).

One validation lesson generalises beyond this renderer and is worth stating: an environment-map
\emph{orientation} error is invisible to a constant or isotropic test scene and to an equatorial view;
only a full-sphere \emph{directional} comparison can expose it. Designing the test to be sensitive to
the quantity under check---rather than to a convenient one---is what makes a differential method
trustworthy.

With the features validated to within the stated residuals, \emph{which} features to enable, and at
what cost in time and quality, becomes a separate question---a performance-engineering one, taken up in
\Cref{ch:optimization}.

\section{The combined showcase}
\label{sec:money-shot}

The features are finally exercised together---the full cloud, under the measured HDR environment, with
forward-scattering Henyey--Greenstein ($g = 0.85$) and MIS direct lighting. Against Mitsuba-analog the
renderer agrees with the reference to within ${\sim}1\,\%$ across four well-separated views and, at equal quality, reaches it
in less time while remaining free of the fireflies that the reference exhibits on this scene
(quantified in \Cref{ch:results}). This
combined result is the correctness foundation on which the performance study of \Cref{ch:optimization}
builds: every optimisation there is required to preserve this image, and is reverted if it does not
(\Cref{fig:showcase}).

\begin{figure}[htp]
  \centering
  \includegraphics[width=\linewidth]{figures/showcase.pdf}
  \caption{The combined showcase: the full cloud under a measured HDR environment with forward
  Henyey--Greenstein scattering ($g = 0.85$). \emph{Left:} our MIS renderer; \emph{right:} Mitsuba's
  only unbiased mode (analog)---each a single \num{64}-spp frame (our frame $\approx\SI{9}{\second}$;
  the Mitsuba-analog frame $\approx\SI{9}{\second}$ steady-state, $\approx\SI{13.5}{\second}$ wall including JIT start-up). The two agree on the converged image (means \num{0.3214} versus
  \num{0.3201}; the systematic brightness check is \Cref{fig:g1-bias}), yet at this budget ours is
  firefly-free while the reference's analog mode is dominated by fireflies---the difference is variance,
  not bias, and its equal-quality cost is quantified in \Cref{ch:results}.}
  \label{fig:showcase}
\end{figure}
